import * as ts from 'typescript';

export class SchemaLinker {
    constructor(program: ts.Program, checker: ts.TypeChecker) {
        return;
    }
    public flatten(file: string, name: string) {
        return;
    }
}
